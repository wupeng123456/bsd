import React from 'react';
import { connect } from 'dva';
function IndexPage() {
    return (
        <div>
            123456
        </div>
    );
}
IndexPage.propTypes = {
};
export default connect()(IndexPage);
